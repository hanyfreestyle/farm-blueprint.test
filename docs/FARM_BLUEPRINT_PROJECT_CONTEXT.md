# Farm Blueprint — Project Context & Working Rules

## 1. تعريف المشروع

المستودع الأساسي:

`hanyfreestyle/farm-blueprint.test`

هذا المشروع **ليس نظام إدارة مزرعة الأرانب النهائي**.

وظيفته بناء **Blueprint / دراسة تحليلية تفصيلية** قبل تطوير النظام الفعلي، وتحويل التصور ودورة العمل إلى:

**أقسام → أقسام فرعية → أسئلة → إجابات → قرارات ومتطلبات → تقارير فنية → Blueprint قابل للتنفيذ.**

الهدف من الأسئلة هو الوصول إلى **Software Requirements وBusiness Rules** موثقة، وليس جمع معلومات عامة فقط.

---

## 2. التقنية المستخدمة

- Laravel 12
- PHP 8.2
- Filament 4
- MySQL
- Questionnaire Engine مخصص
- `spatie/laravel-translatable` للبيانات متعددة اللغات عند الحاجة

---

## 3. الهيكل المنطقي للدراسة

الأقسام الرئيسية الحالية بالترتيب المعتمد:

1. إدارة البيانات الأساسية — Master / Reference Data.
2. هيكل المزرعة — Farm Structure.
3. إعدادات التشغيل ودورة الإنتاج — Operation Settings.
4. تكوين وإدخال القطيع — Herd Setup.
5. الحركات ودورة التشغيل الفعلية — Workflow.
6. التقارير والإشعارات ومؤشرات الأداء — Reports.

الفرق الأساسي بين أول قسمين:

```text
Master / Reference Data
= قوائم وتعريفات مرجعية يعاد استخدامها داخل النظام

Farm Structure
= كيانات فعلية تمثل المزرعة ومواقع التشغيل داخلها
```

---

## 4. إدارة البيانات الأساسية وهيكل المزرعة

### 4.1 إدارة البيانات الأساسية — Master / Reference Data

```text
إدارة البيانات الأساسية
│
├── الأنشطة التشغيلية
├── ملفات إعدادات التشغيل
├── الأغراض الإنتاجية
├── مؤشرات السلالات
├── بيانات السلالات
├── المستخدمون وفريق التشغيل
├── أسباب النقل
├── أسباب النفوق
├── أسباب الاستبعاد
├── أسباب الخروج
├── أسباب تغيير الذكر
├── المحافظات
├── المدن
├── أنظمة التهوية
├── أنظمة التبريد
└── أنظمة التدفئة
```

Seeder المسؤول:

`database/seeders/Sections/QuestionnaireMasterDataSectionSeeder.php`

هذا القسم لا يضم الكيانات الفيزيائية الفعلية للمزرعة.

### 4.2 هيكل المزرعة — Farm Structure

```text
هيكل المزرعة
│
├── بيانات المزرعة
├── بيانات العنبر
├── بيانات البطارية
└── بيانات القفص / العين
```

Seeder المسؤول:

`database/seeders/Sections/QuestionnaireFarmStructureSectionSeeder.php`

العلاقة الهيكلية الأساسية:

```text
Farm
  ↓
Barn
  ↓
Battery
  ↓
Cage / Cell
```

هذه الكيانات تمثل مواقع فعلية وليست Lookup Lists.

### قاعدة الحفاظ على الهوية عند إعادة التنظيم

إذا كان Subsection هيكلي موجودًا بالفعل تحت Parent قديم، لا يتم حذفه وإعادة إنشائه.

يجب إعادة استخدام **نفس سجل `questionnaire_sections`** وتغيير `parent_id` فقط، بحيث نحافظ قدر الإمكان على:

```text
Section ID
    ↓
Question IDs
    ↓
Answers / Reviews / Relationships
```

`QuestionnaireFarmStructureSectionSeeder` مصمم للبحث عن القسم الموجود وإعادة استخدامه، ويوقف التنفيذ إذا وجد أكثر من سجل بنفس الاسم بدل اختيار سجل عشوائيًا.

---

## 5. طريقة إنشاء الأقسام

المصدر الأساسي للأقسام:

`database/seeders/Sections/`

ويتم استدعاؤها من:

`QuestionnaireSectionSeeder`

الترتيب الرئيسي canonical يتم تثبيته في `QuestionnaireSectionSeeder` بعد تشغيل Seeders الأقسام.

كل Section Seeder مسؤول عن:

- القسم الرئيسي.
- وصفه.
- الأقسام الفرعية.
- وصف كل قسم فرعي وترتيبه.

يجب الحفاظ على هوية الأقسام الحالية قدر الإمكان، خاصة بعد وجود أسئلة أو إجابات مرتبطة بها.

---

## 6. بيانات المزرعة — قرارات تأسيسية

`بيانات المزرعة` أصبحت جزءًا من:

`هيكل المزرعة`

وليست Master Data مرجعية.

عند بناء أسئلة بيانات المزرعة يجب وجود سؤالين أساسيين على الأقل:

1. ما البيانات التي يجب أن يحتوي عليها ملف المزرعة؟
2. أي من هذه البيانات يجب أن يكون إلزاميًا؟

الهدف:

- السؤال الأول → Fields / Relationships المطلوبة.
- السؤال الثاني → Required / Nullable / Validation Rules.

### الموقع الجغرافي

الموقع المطلوب وظيفيًا يتكون من:

- المحافظة.
- المدينة.
- العنوان.
- الموقع على الخريطة.

### بيانات الاتصال

يجب دعم:

- أكثر من رقم هاتف.
- رقم واتساب أو أكثر عند الحاجة.

لا يتم افتراض أن التصميم النهائي سيكون `phone_1`, `phone_2`؛ المطلوب هو دعم التعدد وظيفيًا، ويمكن أن يتحول إلى Relationship مستقل.

### المالك والمسؤولون

لا يوجد احتياج حالي لتأسيس "مالك المزرعة" كحقل تشغيلي رئيسي.

المسؤولون عن التشغيل هم Users يمكنهم تسجيل الدخول إلى لوحة التحكم، ويتم التعامل معهم في قسم المستخدمين وفريق التشغيل.

### النشاط

تم إلغاء اعتبار نشاط المزرعة Field أو Relationship مباشر على المزرعة.

لا يظهر `أنشطة المزرعة` ضمن حقول ملف المزرعة، ولا يوجد Requirement يلزم المزرعة باختيار نشاط عند إنشائها.

الأنشطة الفعلية للمزرعة **مشتقة من أنشطة العنابر التابعة لها**.

---

## 7. الأنشطة التشغيلية

تم اعتماد الأنشطة كـMaster Data مستقلة:

- Module مستقل.
- Table مستقل في النظام النهائي.
- قابلة للإدارة من لوحة التحكم.
- الاسم `JSON / translatable` بالعربية والإنجليزية.
- الربط يعتمد على IDs وForeign Keys وليس الأكواد النصية.

القرار الحالي:

> **النشاط التشغيلي يرتبط بالعنبر وليس بالمزرعة.**

المرجع الوظيفي يذكر `نوع/استخدام العنبر` مثل:

- إنتاج.
- فطام.
- تسمين.
- حجر / عزل.
- متعدد الاستخدام.

ويجب تحويل القرارات المتعلقة بنشاط العنبر إلى أسئلة صريحة في قسم `بيانات العنبر`.

أما نشاط المزرعة فيستنتج من أنشطة عنابرها ولا يخزن كاختيار مستقل متعارض مع الواقع التشغيلي.

---

## 8. ملفات إعدادات التشغيل

تم إنشاء Subsection مستقل باسم:

`ملفات إعدادات التشغيل`

الاتجاه التصميمي الحالي:

```text
Operational Settings Profile
        ↓
      Barn
```

أي أن الإعدادات **ليست إعدادات على مستوى المزرعة**.

يمكن إنشاء أكثر من ملف إعدادات تشغيل، ويُستخدم ملف مناسب مع العنبر.

القرارات التفصيلية تحسم بأسئلة صريحة حتى تظهر كRequirements في التقرير والـBlueprint النهائي.

---

## 9. المستخدمون وفريق التشغيل والورديات

هذا الجزء من الـFoundation لأنه يؤثر على التشغيل والمهام والتنبيهات والتدقيق.

المبادئ الحالية:

- عضو فريق التشغيل هو User لديه Login.
- يمكن ربط المستخدم بالمزرعة أو المزارع التي يعمل عليها.
- له Role / Permissions.
- يجب دعم Shifts / الورديات.
- ربط المستخدم بالمزرعة يساعد لاحقًا في تحديد نطاق الوصول لما يتبعها.
- يجب الانتباه للسجل التاريخي إذا تغيرت وردية المستخدم مستقبلًا.

التفاصيل النهائية تحسم بأسئلة صريحة.

---

## 10. المحافظات والمدن

المحافظات والمدن Master Data مستقلة.

```text
Governorate
    ↓ 1:N
City
```

كل واحدة Module / Table مستقل، والمدينة ترتبط بالمحافظة عن طريق ID / Foreign Key.

---

## 11. قوائم الأسباب التشغيلية

القوائم التالية Master Data مستقلة:

- أسباب النقل.
- أسباب النفوق.
- أسباب الاستبعاد.
- أسباب الخروج.
- أسباب تغيير الذكر.

كل واحدة:

- Module مستقل.
- Table مستقل في النظام النهائي.
- قابلة للإدارة من لوحة التحكم.
- العلاقات تعتمد على IDs / Foreign Keys.
- لا تعتمد على `code` كهوية للعلاقة.

وجود `is_active` يسمح بإيقاف قيمة بدون حذفها وكسر السجل التاريخي.

---

## 12. قاعدة تعدد اللغات

النظام النهائي عربي / إنجليزي في لوحة التحكم والمخرجات التي تحتاج ترجمة.

أي اسم أو وصف إداري قابل للعرض باللغتين يستخدم **Translatable JSON** بدل:

```text
name_ar
name_en
```

الأرقام والتواريخ والـIDs والإحداثيات والقيم التقنية لا تحتاج ترجمة.

---

## 13. قاعدة العلاقات والهوية في النظام النهائي

العلاقات بين كيانات النظام النهائي تعتمد على:

**IDs + Foreign Keys**

ولا تعتمد على الأكواد النصية.

يمكن وجود Code لأغراض تشغيلية، لكنه ليس أساس الربط بين الجداول.

---

## 14. الملف المرجعي الوظيفي الأساسي

`تصور_مشروع_الارانب.md`

هو Source of Reference لدورة العمل ومتطلبات النظام.

عند إنشاء أو مراجعة أسئلة:

1. قراءة الجزء المقابل من الملف.
2. الحفاظ على المصطلحات والمنطق الموجود فيه.
3. مراجعة الأسئلة والقرارات الموجودة لمنع التكرار غير المقصود.
4. عدم اختراع متطلبات غير مدعومة وكأنها حقائق.
5. فصل أي بحث خارجي عن المرجع بوضوح.

الملف Reference Only ولا يتم تعديله إلا بطلب صريح.

إذا حسم المستخدم قرارًا وظيفيًا جديدًا أثناء التحليل، فيتم اعتماد القرار الجديد في الـBlueprint حتى لو كان يمثل تطويرًا أو تصحيحًا لتصور أولي مكتوب في المرجع، مع الحفاظ على المرجع نفسه بدون تعديل.

---

## 15. فلسفة إنشاء الأسئلة — قاعدة إلزامية

كل سؤال يجب أن ينتج قرارًا مفيدًا مثل:

- Field.
- Required / Nullable Rule.
- Relationship.
- Workflow Rule.
- Validation.
- State / Status.
- Task / Alert.
- Audit / History Rule.
- Configuration Rule.

**أي Requirement أساسي يجب أن يكون ممثلًا بسؤال صريح داخل الاستبيان، حتى إذا كانت الإجابة تبدو بديهية أو كان اتجاه القرار معروفًا مسبقًا.**

يمكن اعتبار الأسئلة نوعين:

1. Discovery Question.
2. Confirmation Question.

لا يحذف السؤال لمجرد أن إجابته بديهية؛ يحذف فقط إذا كان لا ينتج قرارًا مفيدًا أو إذا كان موضوعًا في القسم الخطأ.

---

## 16. Question Types

الأنواع الحالية في `App\Enums\Questionnaire\QuestionType`:

- `text`
- `textarea`
- `number`
- `date`
- `yes_no`
- `single_choice`
- `multi_choice`
- `select`

لا يتم افتراض نوع غير موجود في الكود.

---

## 17. Dependencies

الـEngine يدعم تقنيًا:

- EQUALS
- CONTAINS

القاعدة الحالية:

> إذا كان السؤال معتمدًا، يظهر دائمًا. وإذا لم نعد نحتاجه، نحذفه من Seeder بدل إخفائه بشرط Dependency.

لا تستخدم Dependencies جديدة إلا إذا كان لها مبرر وظيفي واضح وتم اعتمادها.

---

## 18. Question Seeder Sync

الخدمة الموحدة:

`app/Services/Questionnaire/QuestionSeederSyncService.php`

لكل سؤال `seed_key` ثابت.

هوية السؤال تعتمد على:

```text
section_id + seed_key
```

وهوية Option تعتمد على:

```text
question_id + value
```

ويتم استخدام `updateOrCreate` بدل الحذف وإعادة الإنشاء العمياء.

### Canonical Parent Routing

الكيانات التالية مملوكة لقسم `هيكل المزرعة`:

- بيانات المزرعة.
- بيانات العنبر.
- بيانات البطارية.
- بيانات القفص / العين.

`QuestionSeederSyncService` يحتوي Canonical Parent Mapping لهذه الأقسام حتى لا يستطيع Seeder قديم توجيهها إلى Master Data أو إنشاء مسار متعارض.

---

## 19. أوضاع المزامنة والتعامل مع الإجابات

`QuestionSeederSyncService` يدعم:

```text
prune
preserveAnswers
```

### أثناء مرحلة إعادة البناء الحالية

المستخدم يعتمد حاليًا محليًا على:

```bash
php artisan migrate:refresh --seed
```

وهذا يعني أن قاعدة بيانات التطوير تعاد بناؤها بالكامل، وبالتالي **الإجابات الحالية لا تعتبر محفوظة عبر هذا الأمر**.

مع ذلك يجب كتابة Seeders من الآن بصورة مستقرة حتى لا نضطر لإعادة تصميمها عندما تصبح الإجابات مهمة.

### عند بدء الحفاظ على الإجابات لاحقًا

نتوقف عن استخدام `migrate:refresh --seed` كطريقة تشغيل عادية على قاعدة البيانات التي تحتوي الإجابات المهمة.

ويصبح المبدأ:

```text
Stable Section Record
+ Stable seed_key
+ Stable option value
+ preserveAnswers = true
```

أي تغيير تدميري يجعل إجابة موجودة غير صالحة يجب أن يوقف الـSync برسالة واضحة بدل حذف البيانات.

### نقل الأقسام بعد وجود إجابات

النقل المسموح به هو تغيير `parent_id` لنفس Section record مع الحفاظ على الـID.

غير المسموح:

```text
Delete subsection
→ Create new subsection
→ Recreate questions
```

لأن ذلك يغير الهوية ويعرض الإجابات والعلاقات للخطر.

---

## 20. شكل Question Seeder

كل Seeder يكون في الأساس تعريفًا للأسئلة ثم يستخدم `QuestionSeederSyncService`.

مثال canonical لبيانات المزرعة:

```php
app(QuestionSeederSyncService::class)->sync(
    mainSectionName: 'هيكل المزرعة',
    sectionName: 'بيانات المزرعة',
    questions: $questions,
    prune: true,
    preserveAnswers: true, // عند مرحلة تثبيت الإجابات
);
```

لا يكرر كل Seeder منطق البحث والحذف ومقارنة الإجابات ومزامنة Options.

---

## 21. Orchestrators الحالية للأسئلة

تم فصل Seeders الأسئلة إلى مجموعتين واضحتين:

### Reference / Master Data

`database/seeders/QuestionnaireMasterDataQuestionsSeeder.php`

يستدعي الأسئلة المرجعية المعتمدة فقط.

### Farm Structure

`database/seeders/QuestionnaireFarmStructureQuestionsSeeder.php`

يستدعي حاليًا:

- `FarmDataQuestionsSeeder`
- `BarnDataQuestionsSeeder`
- `BatteryDataQuestionsSeeder`

ولا يستدعي `CageDataQuestionsSeeder` القديم لأن أسئلة القفص قيد إعادة البناء وفق النموذج الجديد.

يوجد مؤقتًا:

`QuestionnaireFarmQuestionsSeeder`

كـCompatibility Wrapper للاختبارات أو الأوامر القديمة، ويستدعي المجموعتين معًا. لا يستخدم كمرجع معماري جديد.

---

## 22. إعادة بناء قاعدة البيانات أثناء مرحلة التطوير

طريقة العمل المحلية الحالية المعتمدة من المستخدم هي:

```bash
php artisan migrate:refresh --seed
```

الأمر **تدميري** ويعيد بناء قاعدة البيانات من الصفر، لذلك يستخدم الآن فقط لأن المستخدم قرر أن إعادة الإجابة مقبولة أثناء إعادة تنظيم الدراسة.

`DatabaseSeeder` يجب أن يبني الحالة الكاملة المتوقعة من الصفر وألا يعتمد على أسئلة أو Sections باقية من تشغيل سابق.

عندما تصبح الإجابات الحالية بيانات يجب الحفاظ عليها، لا يستخدم `refresh` أو `fresh` عليها، ويتم الانتقال إلى تشغيل Seeders المزامنة بصورة محافظة مع `preserveAnswers = true` حسب القسم.

---

## 23. دورة البيانات

```text
التصور العام
      ↓
الأقسام
      ↓
الأسئلة
      ↓
الإجابات
      ↓
المراجعة
      ↓
Software Requirements / Business Rules
      ↓
Technical Report
      ↓
Implementation Preparation Report
      ↓
Blueprint
      ↓
النظام النهائي لاحقًا
```

---

## 24. منهج العمل عند بناء أسئلة قسم جديد

```text
1. قراءة الجزء المقابل من تصور_مشروع_الارانب.md
↓
2. مراجعة Section/Subsection الحالي
↓
3. مراجعة الأسئلة والقرارات السابقة
↓
4. استخراج Requirements التي يجب إثباتها أو اكتشافها
↓
5. تصميم الأسئلة المستقلة
↓
6. إعطاء seed_key ثابت لكل سؤال
↓
7. تحديد Question Type
↓
8. تحديد Options وقيمها المستقرة
↓
9. تحديد report_category
↓
10. تحديد target_entity
↓
11. مراجعة التغطية والترابط مع الأقسام التالية
↓
12. استخدام QuestionSeederSyncService للمزامنة
↓
13. تنفيذ GitHub فقط بطلب صريح
```

---

## 25. قواعد GitHub الإلزامية

المستودع يتم التعامل معه افتراضيًا كـ:

**READ ONLY**

يسمح بالقراءة والبحث والتحليل والمراجعة والاقتراح فقط.

لا يتم تعديل أو إنشاء أو حذف ملف، ولا Commit أو Push أو Branch أو Pull Request أو Seeder، إلا بطلب صريح ومباشر من المستخدم.

**وجود صلاحية تقنية لا يعني وجود إذن بالتعديل.**

`تصور_مشروع_الارانب.md` Reference Only ولا يتم تعديله إلا بطلب صريح.

---

## 26. مزامنة GitHub مع النسخة المحلية

المستخدم يعمل على نسخة Local.

بعد انتهاء تعديلات المرحلة:

```bash
git status
git pull origin master
```

ثم في مرحلة إعادة البناء الحالية:

```bash
php artisan migrate:refresh --seed
```

يفضل سحب تعديلات المرحلة معًا بعد اكتمالها، مع الانتباه لأي تعديلات Local غير محفوظة.

---

## 27. المبدأ الأساسي

هذا المشروع هو:

**أداة تحليل متطلبات قبل التطوير**

وليس:

**نظام إدارة المزرعة النهائي.**

الهدف الوصول إلى Blueprint موثق ومنظم يقلل الافتراضات والقرارات غير المثبتة قبل بدء بناء النظام الحقيقي.

---

## 28. دليل تفسير الأسئلة ومسار Final Requirements — قاعدة معتمدة

تم اعتماد طبقة تفسير مستقلة بين الأسئلة والإجابات وبين وكيل كتابة الـRequirements.

ملفات `questionnaire-guide`:

- تشرح معنى الأسئلة وحدود تفسيرها.
- تشير إلى الأسئلة باستخدام `Question Key / seed_key`.
- لا تكرر الإجابات الفعلية حتى لا يصبح لدينا مصدران للحقيقة.
- توضح ما الذي يجب أن ينتج من الإجابة من Requirements / Rules / Relationships / Validation.
- توضح ما الذي لا يجوز استنتاجه من السؤال.

### ملاحظة تنظيمية بعد فصل Farm Structure

تم فصل Farm / Barn / Battery / Cage في بنية الـSections والـQuestion Seeder Orchestrators.

ملفات Guide التاريخية التي كانت تحت `questionnaire-guide/master-data/` لا يتم حذفها أو نقلها بصورة عمياء. يعاد تنظيمها في خطوة توثيقية مستقلة مع الحفاظ على المحتوى والروابط، بعد التأكد من نجاح بنية الأقسام الجديدة.

### Question Key في التقارير

يجب أن يظهر `seed_key` في تقارير الدراسة باسم:

```text
Question Key: farm.stopped_behavior
```

وهو مفتاح الربط بين:

```text
Questionnaire Guide
        ↕
Exported Answers
        ↓
Requirements Agent
```

### Final Requirements Input

الـFinal Requirements Input:

- يستبعد أسئلة `manual_review`.
- يستبعد أسئلة `*.additional_requirements` كمدخل مباشر للـRequirements.
- لا يعتمد سؤالًا نهائيًا بلا إجابة.
- لا يعتمد سؤالًا نهائيًا بلا `Question Key`.
- لا يعتمد إجابة عليها مراجعة غير محسومة.

### المصادر المسموح بها لوكيل كتابة الـRequirements

بعد اكتمال الأسئلة والإجابات، يقرأ وكيل كتابة الـRequirements فقط:

1. `docs/FARM_BLUEPRINT_PROJECT_CONTEXT.md`.
2. ملف Guide الخاص بالقسم داخل `docs/questionnaire-guide/`.
3. Final Requirements Input.
4. `docs/questionnaire-guide/REQUIREMENTS_CONFLICTS.md`.

**لا يستخدم `تصور_مشروع_الارانب.md` في مرحلة كتابة الـRequirements النهائية.**

### قاعدة التعارضات

إذا اكتشف وكيل الـRequirements تعارضًا بين قرارين أو إجابتين:

- لا يحسمه بنفسه.
- لا يخترع حلًا وسطًا.
- لا يرجع إلى المرجع الوظيفي القديم لحسمه.
- يسجله في `docs/questionnaire-guide/REQUIREMENTS_CONFLICTS.md` بكود فريد.
- يوضح Question Keys المتعارضة والأثر والقرار المطلوب.
- يستمر في الأجزاء غير المتأثرة.
- بعد قرار المستخدم يصحح الـRequirement المتأثر ويتحقق من زوال التعارض ثم يحذف الملاحظة المفتوحة.

### مسار البيانات النهائي

```text
تصور_مشروع_الارانب.md
      ↓  يستخدم أثناء بناء الدراسة فقط
Sections / Questions
      ↓
Answers / Review
      ↓
Questionnaire Interpretation Guides
      +
Final Requirements Input
      +
Project Context
      ↓
Requirements Agent
      ↓
Software Requirements / Business Rules
      ↓
Blueprint
      ↓
النظام النهائي لاحقًا
```
